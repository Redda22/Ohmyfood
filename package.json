{
  "name": "ohmyfood",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "compile-sass": "sass --watch sass/style.scss:sass/style.css",
    "compile-sass-restaurant": "sass --watch restaurants/restaurant_SASS/restaurant.scss:restaurants/restaurant_SASS/restaurant.css",
    "start": "concurrently \"npm run compile-sass\" \"npm run compile-sass-restaurant\""
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
